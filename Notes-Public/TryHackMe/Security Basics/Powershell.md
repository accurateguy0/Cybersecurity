Powershell is a tool used for automation, combining scripting ability with .NET OOP features, that was limited with cmd.exe. Windows used API and structured data, while Unix treated everything as text files. 

An object is an item that contains methods and properties.